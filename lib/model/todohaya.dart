class ToDohaya {
  int id;
  String title;
  String description;
  bool done;
  DateTime dueDate;

  ToDohaya(
      {required this.id,
      required this.title,
      required this.description,
      required this.done,
      required this.dueDate});
}
