/*
double calcSalary(int hours, double rate, [double bonus = 0, int overtime = 0]) {
  return (hours + overtime) * rate + bonus ;
}


double calcSalary({int? hours, double? rate, double bonus = 0, int overtime = 0}) {
  return (hours! + overtime) * rate! + bonus ;
}
 */